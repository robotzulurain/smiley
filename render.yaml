services:
  - type: web
    name: smiley
    env: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: python manage.py migrate --noinput && python manage.py createsuperuser --noinput --username admin --email robotzulurain@gmail.com --password AmrAdmin2024! 2>/dev/null || true && gunicorn amr_project.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: smiley-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.9
